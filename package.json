{
    "name": "workstreams",
    "description": "application to managge workstreams",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.608.0"
    },
    "devDependencies": {
        "jest": "^24.7.1"
    },
    "scripts": {
        "test": "jest"
    }
}
